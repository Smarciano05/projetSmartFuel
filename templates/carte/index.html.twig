{% extends 'base.html.twig' %}

{% block title %}Carte {% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('css/carte.css') }}">
        <style>
            #map {
                height: 584px;
                margin-top: 80px;
            }
        </style>

        <!-- Cette div contient la carte -->
    <!-- c'est le conteneur où leaflet va dessiner la carte , on ne met rien car leaflet prend cette div et y injecte la carte dynamiquement -->
        <div id="map"></div>
        <!-- leafet bibliotheque qui permet d'afficher la carte-->


    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.3.1/dist/leaflet.css"
              integrity="sha512-Rksm5RenBEKSKFjgI3a41vrjkw4EVPlJ3+OiI65vTjIdo9brlAacEuKOiQ5OFh7cOI1bkDwLqdLw3Zg0cRJAAQ=="
              crossorigin=""/>
        <link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.3.0/dist/MarkerCluster.css"/>
        <link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.3.0/dist/MarkerCluster.Default.css"/>


        <script src="https://unpkg.com/leaflet@1.3.1/dist/leaflet.js"
                integrity="sha512-/Nsx9X4HebavoBvEBuyp3I7od5tA0UzAxs+j83KgC8PU0kgB4XiK4Lfe4y4cgBtaRJQEIFCW+oC506aPT2L1zw=="
                crossorigin=""></script>
        <script src="https://unpkg.com/leaflet.markercluster@1.3.0/dist/leaflet.markercluster.js"></script>

        <script type="text/javascript">

            //coordonnes de la capital du Mali : Bamako
            var lat = 12.6392;
            var lon = -8.0029;
            var macarte = null;
            var markerClusters;

            var icone = L.icon({
                iconUrl: "{{ asset('images/station-essence(5).png') }}",
                iconSize: [60, 60],
                iconAnchor: [25, 50],
                popupAnchor: [0, -50]
            })

            function initMap() {
                macarte = L.map('map').setView([lat, lon], 11);
                markerClusters = L.markerClusterGroup();

                L.tileLayer('https://{s}.tile.openstreetmap.fr/osmfr/{z}/{x}/{y}.png', {
                    attribution: 'données © <a href="//osm.org/copyright">OpenStreetMap</a>/ODbL - rendu <a href="//openstreetmap.fr">OSM France</a>',
                    minZoom: 1,
                    maxZoom: 20
                }).addTo(macarte);


                //sa demande le fichier json pour recuperer les stations essences
                //ensuite sa prend la reponse brute et sa la converti en json pour pouvoir la manipuler
                // et du coup c avec ce data que e vais creer les markers
                // on a converti le fichier json reponse en data
                fetch('data/stationEssence.geojson').then(function (response){
                    return response.json();
                }).then(function (data){
                //L.geoJSON(data) va parcourir le fichier json et pour chaque point il va creer un marker avec les options de style que j'ai defini
                    // feature = station et latlng = postion de la station
                    L.geoJSON(data, {
                        pointToLayer: function (feature, latlng) {
                            var marker =  L.marker(latlng, {icon: icone});
                            var properties = feature.properties;
                            marker.bindPopup('<div style="font-size: 16px;"><strong>' + " Nom : " +properties.name + '</strong></div>');
                            return marker;
                        }

                    }).addTo(markerClusters);
                    macarte.addLayer(markerClusters);
                }).catch(function (error){
                    console.log('Erreur lors du chargement du fichier GeoJSON :', error);
                });

            }

            window.onload = function () {
                initMap();
            };

        </script>





{% endblock %}
